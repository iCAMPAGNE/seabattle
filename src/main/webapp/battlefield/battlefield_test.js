'use strict';

describe('myApp.battlefield module', function() {

  beforeEach(module('myApp.battlefield'));

  describe('battlefield controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var BattlefieldCtrl = $controller('BattlefieldCtrl');
      expect(BattlefieldCtrl).toBeDefined();
    }));

  });
});